<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:resources="clr-namespace:Lorcaire.Application.Resources.GetResources;assembly=Lorcaire.Application"
    x:Class="Lorcaire.Desktop.Views.ResourcesView">

  <Grid RowDefinitions="Auto,Auto,*" RowSpacing="22">
    <Border
        Grid.Row="0"
        Padding="24"
        Background="#0F151B"
        BorderBrush="#252D36"
        BorderThickness="1"
        CornerRadius="10">
      <StackPanel Spacing="14">
        <TextBlock x:Name="FormTitle"
            Text="Create a resource"
            Foreground="White"
            FontSize="20"
            FontWeight="SemiBold"/>
        <TextBox
            x:Name="ResourceName"
            PlaceholderText="Resource name"
            />
        <TextBox
            x:Name="ResourceCategory"
            PlaceholderText="Category (for example: Book, Course, Tool)"
            />
        <TextBox
            x:Name="ResourceDescription"
            PlaceholderText="Description (optional)"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            MinHeight="80"
            />
        <StackPanel Orientation="Horizontal" Spacing="10"><Button
            x:Name="CreateResourceButton"
            Content="Create resource"
            Click="CreateResource"
            HorizontalAlignment="Left"
            Padding="22,10"
            Background="#E8B04A"
            Foreground="#080C11"/><Button x:Name="SaveResourceButton" Content="Save changes" Click="SaveResource" IsVisible="False" Padding="22,10" Background="#E8B04A" Foreground="#080C11"/><Button x:Name="CancelResourceButton" Content="Cancel" Click="CancelEdit" IsVisible="False" Padding="22,10" Background="#20262D" Foreground="White"/><Button x:Name="ConfirmDeleteButton" Content="Confirm delete" Click="ConfirmDelete" IsVisible="False" Padding="22,10" Background="#A63D49" Foreground="White"/><Button x:Name="CancelDeleteButton" Content="Cancel" Click="CancelDelete" IsVisible="False" Padding="22,10" Background="#20262D" Foreground="White"/></StackPanel>
        <TextBlock
            x:Name="OperationMessage"
            Foreground="#929AA5"
            TextWrapping="Wrap"/>
      </StackPanel>
    </Border>

    <TextBlock
        Grid.Row="1"
        Text="Your resources"
        Foreground="White"
        FontSize="20"
        FontWeight="SemiBold"/>

    <ScrollViewer Grid.Row="2">
      <ItemsControl x:Name="ResourcesList">
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="resources:ResourceSummary">
            <Border
                Margin="0,0,0,12"
                Padding="20"
                Background="#0F151B"
                BorderBrush="#252D36"
                BorderThickness="1"
                CornerRadius="10">
              <Grid ColumnDefinitions="*,Auto" ColumnSpacing="18"><StackPanel Spacing="7">
                <TextBlock
                    Text="{Binding Category}"
                    Foreground="#E8B04A"
                    FontSize="12"
                    FontWeight="SemiBold"/>
                <TextBlock
                    Text="{Binding Name}"
                    Foreground="White"
                    FontSize="17"
                    FontWeight="SemiBold"/>
                <TextBlock
                    Text="{Binding Description}"
                    Foreground="#929AA5"
                    TextWrapping="Wrap"/>
              </StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center"><Button Content="Edit" Tag="{Binding Id}" Click="BeginEdit" Padding="14,8" Background="#20262D" Foreground="White"/><Button Content="Delete" Tag="{Binding Id}" Click="DeleteResource" Padding="14,8" Background="#3A2024" Foreground="#F2A0A7"/></StackPanel></Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
